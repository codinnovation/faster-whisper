version: '3.8'

services:
  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis_data:/data
    # No ports exposed to host for security, only accessible within Docker network

  api:
    build: .
    restart: always
    expose:
      - "9000"
    volumes:
      - app_data:/app/data
    environment:
      - REDIS_URL=redis://redis:6379/0
      - UPLOAD_DIR=/app/data
      - API_SECRET=${API_SECRET}
    depends_on:
      - redis

  worker:
    build: .
    restart: always
    # Optimized for 4 vCPU server (Total system threads = 4)
    command: celery -A worker.celery_app worker --loglevel=info --concurrency=1 --prefetch-multiplier=1
    volumes:
      - app_data:/app/data
    environment:
      - REDIS_URL=redis://redis:6379/0
      - MODEL_SIZE=small
      - DEVICE=cpu
      - COMPUTE_TYPE=int8
      - CPU_THREADS=2
    depends_on:
      - redis

  worker-2:
    build: .
    restart: always
    command: celery -A worker.celery_app worker --loglevel=info --concurrency=1 --prefetch-multiplier=1
    volumes:
      - app_data:/app/data
    environment:
      - REDIS_URL=redis://redis:6379/0
      - MODEL_SIZE=small
      - DEVICE=cpu
      - COMPUTE_TYPE=int8
      - CPU_THREADS=2
    depends_on:
      - redis

volumes:
  redis_data:
  app_data:
